@{
    ViewData["Title"] = "Job Durumu Takibi";
}

@* Anti-forgery token ekle *@
@Html.AntiForgeryToken()

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Aktif İşlemler</h3>
                    <button class="btn btn-primary btn-sm float-right" onclick="refreshJobs()">Yenile</button>
                </div>
                <div class="card-body">
                    <div id="jobList">
                        <!-- Jobs will be loaded here -->
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
function refreshJobs() {
    fetch('/Admin/GetAllJobStatuses')
        .then(response => response.json())
        .then(data => {
            updateJobList(data);
        });
}

function updateJobList(jobs) {
    const jobList = document.getElementById('jobList');
    
    // jobs null veya undefined kontrolü
    if (!jobs || Object.keys(jobs).length === 0) {
        jobList.innerHTML = '<p class="text-muted">Şu anda aktif işlem yok.</p>';
        return;
    }
    
    let html = '';
    let index = 0;
    // Dictionary olarak işle (Object.entries kullan)
    Object.entries(jobs).forEach(([jobId, status]) => {
        // Status kontrolü - GENİŞLETİLMİŞ MAPPING
        const jobStatus = status.Status || 'Unknown';
        const jobMessage = status.Message || 'Mesaj yok';
        const jobProgress = status.Progress || 0;
        
        // Tarih kontrolü ve formatı - İYİLEŞTİRİLMİŞ
        let formattedDate = 'Bilinmiyor';
        if (status.UpdatedAt) {
            try {
                // ISO string veya .NET tarih formatını handle et
                let dateStr = status.UpdatedAt;
                if (typeof dateStr === 'string' && dateStr.includes('/Date(')) {
                    // .NET JSON tarih formatı: /Date(1234567890000)/
                    const timestamp = parseInt(dateStr.match(/\d+/)[0]);
                    const date = new Date(timestamp);
                    formattedDate = date.toLocaleString('tr-TR');
                } else {
                    const date = new Date(dateStr);
                    if (!isNaN(date.getTime())) {
                        formattedDate = date.toLocaleString('tr-TR');
                    }
                }
            } catch (e) {
                console.error('Tarih formatı hatası:', e, status.UpdatedAt);
                formattedDate = 'Tarih hatası';
            }
        }
        
        // GENİŞLETİLMİŞ STATUS MAPPING
        const progressClass = (jobStatus === 'Running' || jobStatus === 'Queued') ? 'progress-bar-animated' : '';
        const statusClass = jobStatus === 'Running' ? 'badge-primary' : 
                           jobStatus === 'Completed' ? 'badge-success' : 
                           jobStatus === 'Cancelled' ? 'badge-warning' :
                           jobStatus === 'Queued' ? 'badge-info' :
                           jobStatus === 'Failed' ? 'badge-danger' : 'badge-secondary';
        
        // STATUS DISPLAY - Türkçe çeviri
        const statusDisplay = jobStatus === 'Running' ? 'Çalışıyor' :
                             jobStatus === 'Completed' ? 'Tamamlandı' :
                             jobStatus === 'Cancelled' ? 'İptal Edildi' :
                             jobStatus === 'Queued' ? 'Kuyrukta' :
                             jobStatus === 'Failed' ? 'Başarısız' : jobStatus;
        
        html += `
            <div class="job-item border rounded p-3 mb-3">
                <div class="row">
                    <div class="col-md-8">
                        <h5>İşlem #${index + 1} (${jobId.substring(0, 8)}...)</h5>
                        <p class="mb-1">${jobMessage}</p>
                        <small class="text-muted">Son Güncelleme: ${formattedDate}</small>
                    </div>
                    <div class="col-md-4 text-right">
                        <span class="badge ${statusClass} mb-2">${statusDisplay}</span>
                        <div class="progress mb-2">
                            <div class="progress-bar ${progressClass}" style="width: ${jobProgress}%">
                                %${jobProgress}
                            </div>
                        </div>
                        ${(jobStatus === 'Running' || jobStatus === 'Queued') ? `<button class="btn btn-sm btn-danger" onclick="cancelJob('${jobId}')">İptal Et</button>` : ''}
                    </div>
                </div>
            </div>
        `;
        index++;
    });
    jobList.innerHTML = html;
}

function cancelJob(jobId) {
    if (confirm('Bu işlemi iptal etmek istediğinizden emin misiniz?')) {
        // Anti-forgery token ekle
        const token = document.querySelector('input[name="__RequestVerificationToken"]')?.value;
        
        fetch('/Admin/CancelJob', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'RequestVerificationToken': token
            },
            body: JSON.stringify({ jobId: jobId })
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                refreshJobs();
            } else {
                alert('İşlem iptal edilemedi!');
            }
        })
        .catch(error => {
            console.error('Hata:', error);
            alert('Bir hata oluştu!');
        });
    }
}

// Auto refresh every 5 seconds
setInterval(refreshJobs, 5000);

// Initial load
refreshJobs();
</script>